[project]
name = "temperature-monitor"
version = "0.1.0"
description = "Home temperature monitoring application"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "flask>=3.1.1",
    "pandas>=2.3.1",
    "psycopg2>=2.9.10",
]
